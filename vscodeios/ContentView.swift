//
//  ContentView.swift
//  vscodeios
//
//  Created by wheng on 2023/12/29.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Spacer()
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
